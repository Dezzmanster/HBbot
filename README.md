# Birthday Bot

Телеграм бот для автоматических поздравлений с днем рождения участников чата.

## Возможности

- 🎂 Автоматическая отправка поздравлений в заданное время
- 🤖 Генерация уникальных поздравлений с помощью GigaChat
- 🎨 **Генерация персонализированных изображений с помощью Kandinsky API**
- 📱 Отправка красивых визуальных поздравлений в Telegram
- 💾 Хранение информации о пользователях в конфигурационном файле
- ⚙️ Настраиваемые промпты для генерации поздравлений и изображений
- 💬 Поддержка индивидуальных чатов для каждого пользователя
- 🛡️ Fallback механизм при недоступности внешних API
- 📝 Профессиональное логирование всех операций

## Структура проекта

```
HBbot/
├── birthday_bot.py              # Основной модуль бота
├── kandinsky_client.py          # Клиент для работы с Kandinsky API
├── constants.py                 # Константы и настройки
├── users_config.json            # Конфигурация пользователей (не в git)
├── users_config.example.json    # Пример конфигурации
├── birthday_prompt.txt          # Промпт для генерации поздравлений
├── birthday_image_prompt.txt    # Промпт для генерации изображений
├── generated_images/            # Папка для сгенерированных изображений (не в git)
├── .env                         # Переменные окружения (не в git)
├── .gitignore                   # Исключения для git
├── pyproject.toml              # Конфигурация проекта
├── uv.lock                     # Файл блокировки зависимостей
└── README.md                   # Документация
```

## Архитектура

После рефакторинга код имеет четкую структуру:

- **`BirthdayBot`** - основной класс с разделением на приватные методы
- **`KandinskyClient`** - отдельный клиент для работы с Fusion Brain API
- **Константы** вынесены в отдельный файл для удобства настройки
- **Логирование** на всех уровнях для отладки и мониторинга
- **Обработка ошибок** с fallback механизмами при недоступности API
- **Типизация** для лучшей поддержки IDE и предотвращения ошибок
- **Асинхронная архитектура** для эффективной работы с внешними API

## Установка и настройка

### 1. Установка зависимостей

Проект использует `uv` для управления зависимостями:

```bash
uv sync
```

### 2. Настройка переменных окружения

Создайте файл `.env` и заполните его:

```env
# Telegram Bot Token (получить у @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# GigaChat API credentials
GIGACHAT_CREDENTIALS=your_gigachat_credentials_here
GIGACHAT_SCOPE=GIGACHAT_API_PERS

# Kandinsky API credentials (получить на https://fusionbrain.ai/)
KANDINSKY_API_KEY=your_kandinsky_api_key_here
KANDINSKY_SECRET_KEY=your_kandinsky_secret_key_here

# Chat ID чата, куда отправлять поздравления
CHAT_ID=-1001234567890
```

### 3. Настройка пользователей

Скопируйте пример конфигурации и заполните данными:

```bash
cp users_config.example.json users_config.json
```

Отредактируйте файл `users_config.json`:

```json
{
  "birthday_time": "09:00",
  "default_chat_id": -1001234567890,
  "users": [
    {
      "name": "Имя Фамилия",
      "username": "telegram_username",
      "birthday": "DD.MM",
      "chat_id": -1001234567890
    },
    {
      "name": "Другой Пользователь",
      "username": "another_username", 
      "birthday": "DD.MM",
      "chat_id": -1009876543210
    },
    {
      "name": "Третий Пользователь",
      "username": "third_username",
      "birthday": "DD.MM"
    }
  ]
}
```

Где:
- `birthday_time` - время отправки поздравлений в формате ЧЧ:ММ (по умолчанию 09:00)
- `default_chat_id` - chat ID по умолчанию для пользователей без индивидуального chat_id
- `name` - полное имя пользователя
- `username` - имя пользователя в Telegram (без @)
- `birthday` - дата рождения в формате ДД.ММ
- `chat_id` - (опционально) индивидуальный chat ID для конкретного пользователя

**Приоритет выбора chat_id:**
1. Индивидуальный `chat_id` в настройках пользователя
2. `default_chat_id` из конфигурации
3. `CHAT_ID` из `.env` файла

**Важно:** 
- Файл `users_config.json` добавлен в `.gitignore` и не попадает в репозиторий
- Используйте `users_config.example.json` как шаблон для создания своего конфига
- Можно отправлять поздравления в разные чаты для разных пользователей

### 4. Настройка промптов

- Отредактируйте файл `birthday_prompt.txt` для изменения стиля текстовых поздравлений
- Отредактируйте файл `birthday_image_prompt.txt` для изменения стиля генерируемых изображений

**Примечание:** Если Kandinsky API недоступен или не настроен, бот автоматически будет отправлять только текстовые поздравления.

## Запуск

```bash
uv run birthday_bot.py
```

Бот будет:
1. Сразу проверит, есть ли именинники сегодня
2. Для каждого именинника сгенерирует персонализированное изображение (если Kandinsky API настроен)
3. Сгенерирует уникальное текстовое поздравление с помощью GigaChat
4. Отправит красивое визуальное поздравление в Telegram
5. Запустит планировщик для ежедневной проверки в указанное время

## Логирование

Бот ведет подробные логи работы:
- **Консоль** - основные события и ошибки
- **Файл `birthday_bot.log`** - полная информация для отладки
- Уровни логирования: INFO, WARNING, ERROR, CRITICAL
- Автоматическая ротация и кодировка UTF-8

Примеры логов:
```
2024-01-15 09:00:01 - __main__ - INFO - Проверяем неотправленные поздравления...
2024-01-15 09:00:02 - __main__ - INFO - Генерируем изображение для Иван...
2024-01-15 09:00:25 - kandinsky_client - INFO - Изображение успешно сгенерировано: generated_images/birthday_Иван_1642233625.png
2024-01-15 09:00:28 - __main__ - INFO - поздравление отправлено для Иван в чат -1001234567890
2024-01-15 09:00:29 - __main__ - INFO - Бот запущен! Проверка дней рождения каждый день в 09:00
```

## Получение API ключей

### Chat ID для Telegram

Для получения ID чата:
1. Добавьте бота в нужный чат
2. Отправьте любое сообщение в чат
3. Перейдите по ссылке: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. Найдите в ответе поле `"chat":{"id": -1001234567890}`
5. Добавьте найденный ID в `.env` файл как `CHAT_ID=-1001234567890`

### Kandinsky API ключи

Для получения ключей Kandinsky:
1. Зарегистрируйтесь на [fusionbrain.ai](https://fusionbrain.ai/)
2. Перейдите в раздел API
3. Получите `API Key` и `Secret Key`
4. Добавьте их в `.env` файл:
   ```env
   KANDINSKY_API_KEY=your_api_key_here
   KANDINSKY_SECRET_KEY=your_secret_key_here
   ```

**Примечание:** Ключи Kandinsky опциональны. Если они не настроены, бот будет работать без генерации изображений.

## Возможные проблемы

### Проблемы с uv
Если возникают ошибки с uv (например, `SIGKILL`), используйте альтернативный способ:

```bash
# Очистите кеш uv
uv cache clean

# Или используйте стандартный venv
python3 -m venv .venv
source .venv/bin/activate
python -m ensurepip --upgrade
python -m pip install langchain-gigachat python-dotenv python-telegram-bot schedule aiofiles aiohttp requests
```

### Особенности генерации изображений

- **Время генерации:** ~20-40 секунд на одно изображение
- **Размер файлов:** ~1-2 МБ в формате PNG
- **Автоочистка:** Старые изображения удаляются через 7 дней
- **Fallback:** При недоступности API отправляется только текстовое поздравление
- **Персонализация:** Каждое изображение генерируется с учетом имени именинника
- **Папка хранения:** `generated_images/` (автоматически создается, не попадает в git)
